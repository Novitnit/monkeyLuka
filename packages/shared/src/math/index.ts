/**
 * Barrel entry point for the shared math-notation parser: the ASCII → math
 * AST stage of the quest question typesetter (the Phaser rendering lives in
 * `apps/web/src/game/quest/math-format.ts`).
 */
export * from "./parse";