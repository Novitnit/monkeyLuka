# Dependencies
node_modules/

# Next.js (apps/web keeps its own too)
.next/
out/

# Builds / temp
dist/
build/
*.tsbuildinfo

# Runtime data (the runs SQLite lives in the repo-root data/ dir — the
# compose bind mount ./data:/app/data)
data/

# Env files
.env
.env.*
!.env.example

# Logs & OS
*.log
.DS_Store

# Tiled editor (Assets/map: keep the .tiled-project config, skip session UI state)
*.tiled-session
